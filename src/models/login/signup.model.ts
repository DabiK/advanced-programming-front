export interface SignupCredential {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
    profession: string;
    password: string;
    confirmPassword: string;
}
